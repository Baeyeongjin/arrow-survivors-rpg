# 다음 작업 인수인계 — M5-D 마왕성 세로 슬라이스

> 최종 갱신: 2026-07-30
> 기준 브랜치: `master`
> 저장소: `Baeyeongjin/arrow-survivors-rpg`

## Claude에게 바로 전달할 요청문

> 미커밋 WIP는 없다. `RPG_ROGUELIKE_EVOLUTION_PLAN.md`의 M5 항목을 읽고 아래 **M5-D 마왕성 세로 슬라이스**를 구현해라. 마왕성은 목표를 하나 더 반복하는 던전이 아니라 최종 빌드를 검증하는 전투 관문 던전이다. 구현 후 신규 `tests/CastleSliceTest.gd`와 기존 24개 테스트, 메인 흐름 3종을 통과시키고 `--castle-preview=` 실제 렌더까지 확인한 다음 커밋·푸시해라.

## 현재 완료 상태

다음 항목은 이미 구현·검증됐으므로 다시 만들거나 되돌리지 않는다.

- 던전 월드 크기: `3840×3840`. 5개 맵 연결성·목표 좌표 테스트 완료.
- 공격 애니메이션: 플레이어 11종, 일반 몬스터 22종 완료.
- 걷기 애니메이션: 플레이어 11종, 일반 몬스터 22종 완료.
- 고유 키와 원본 스프라이트명이 다른 정예·중간보스 8종은 `Enemy.setup()`이 **스프라이트 파일명**으로 애니 폴더를 찾아 원본 프레임을 재사용한다. 프레임 폴더를 복제하지 않는다.
- M3 지옥, M5-A 묘지, M5-B 빙하, M5-C 공허 세로 슬라이스 완료.
- M4 3층 원정, 경로 선택, 장비 추출, 자동 분해, 보스 파편 완료.
- 5개 던전의 이동 불가 외곽은 PixelLab 테마 전용 재질 30장(`assets/maps/<theme>/outer/`)으로 굽는다. 128px 원본을 64px로 줄인 뒤 nearest 정수 2배 확대해 32px 바닥과 같은 도트 굵기를 맞춘다. 32px 줄무늬·체크 반복과 공허의 완전한 검정 공백으로 되돌리지 않는다.
- 외곽 주재질의 128px 윤곽 반복은 **180도 회전 두 방향**을 5:5로 섞어 끊는다. **좌우 또는 상하만 뒤집는 4방향으로 되돌리지 않는다** — 이웃 타일과 거울축이 생겨 나비 무늬 벽지가 된다(빙하가 가장 심했다. 실제 렌더로 확인·폐기).
- 일반 적 스폰은 화면 밖 72px 이상, 벽에서 중심 기준 54px 이상 여유가 있는 실제 바닥 후보만 쓴다. 막힌 지점을 벽 가장자리에 투영하던 `_edge_pos()`는 삭제했다.
- 원정 1층은 기존 수치, 2·3층은 체력 `×1.30/×1.68`, 공격력 `×1.16/×1.34`, 속도 `×1.05/×1.10`, 출현 압박 `×1.14/×1.30`, 정예 `+2.5%p/+6%p`를 적용한다.
- 박쥐 무리·원진·벽·포위·협공처럼 적을 대량 생성하던 뱀서식 돌발 대형은 삭제 완료. 분 단위 사건은 12~14초 지역 위협 6종으로 현재·신규 일반/정예 몬스터를 일시 강화하며, 붉은 몸 색조·상단 HUD·최대 8 G 극복 보상을 제공한다. 수제 중간보스·최종보스는 제외하며 구 대형을 다시 도입하지 않는다.
- 자동공격에 사거리를 걸었다. `Main.ATTACK_RANGE_BASE = 360`, 하드 상한 `ATTACK_RANGE_MAX = 560`, 실제 사거리는 `min(560, 360 × _area_scale())`. 사거리 안에 적·보스·목표·파괴물이 하나도 없으면 무기가 발사되지 않고 쿨다운도 새로 돌지 않아, 표적이 들어오는 순간 첫 발이 나간다. 발사가 곧 `player.play_attack()`이므로 공격 모션도 같은 게이트를 따른다. 다중 조준 무기 5종과 `_nearest_list()`는 `_targets_by_distance()`로 사거리 밖 표적을 걸러낸다. **사거리 무제한으로 되돌리지 않는다.**
- 난이도 3차 상향(사장님 "아직도 너무 쉬움"): 몬스터 체력 ×2.2 → ×2.8, 접촉 피해 ×1.45 → ×1.75, 레벨업 요구 XP ×2.5 → ×3.6, 필드 골드는 정예 `5+2N` → `3+N` / 일반 확률 0.30 → 0.16 · 값 `xp/4` → `xp/6`.
- 로컬 런 텔레메트리 완료. 단, 아직 실제 수동 원정 표본은 부족하다.
- 독립 회귀 테스트 24개와 메인 흐름 3개가 모두 통과한다.

## 아직 해결하지 않은 것

- 3차 난이도 상향과 사거리 제한의 실제 수치는 **아직 손맛 확인 전이다.** 조정 노브는 `Main.ATTACK_RANGE_BASE` / `ATTACK_RANGE_MAX`, `Enemy.setup`의 체력·접촉 피해 배수, `Main._xp_requirement`의 곱, `on_enemy_killed`의 골드 값 네 곳이다. `tests/AttackRangeTest.gd`가 사거리 곡선·상한·XP 단조성·몬스터 배수를 잠그므로 수치를 바꾸면 이 테스트도 같이 고친다.

- `--autoshot --selected-stage=3 --map`이 120초 동안 종료되지 않는다. 원인 미조사. 자동 캡처 경로에서만 재현되며 테스트 23개와 메인 흐름 3종에는 영향이 없다. 장시간 멈추면 새로 생긴 Godot 자식 프로세스의 PID만 확인해 종료하고 사용자 편집기 프로세스는 건드리지 않는다.
- 통행 가능 바닥의 Wang 전이 타일이 던전 톤과 어긋난다. 실제 렌더 기준: 묘지는 형광 연두·연보라 테두리, 빙하는 흰 피아노 건반 테두리, 지옥은 청록 테두리, 공허는 자홍 테두리로 보이고 마왕성 바닥은 32px 청록 체커가 그대로 읽힌다. 외곽 재질을 어둡게 바꾼 뒤 대비가 더 벌어졌다. **이 WIP 범위가 아니었고 사장님 판단 대기 중이다.** 손대기 전에 확인받는다.
- 지옥·묘지·빙하·공허 수치는 통계 표본이 모일 때까지 대폭 조정하지 않는다.

## 다음 1순위 — M5-D 마왕성

마왕성은 "탐사 목표를 반복하는 네 번째 던전"보다 **최종 빌드를 검증하는 전투 관문 던전**이어야 한다.

권장 한 층 리듬:

```text
첫 성문 관문
→ 지휘관이 섞인 정예 연속전
→ 중간보스
→ 마지막 성문 관문
→ 다단계 마왕 보스
```

구현 방향:

1. `CastleGate.gd`를 별도 목표 노드로 만든다.
   - 기존 `assets/maps/demon_castle/castle_gate.png`를 사용한다.
   - 단순 체력벽보다 관문 진입 시 짧은 정예 웨이브를 봉쇄전으로 소환하고, 전부 처치하면 개방되는 방식이 적합하다.
   - 목표 위치는 `StageLayout.make(5).objective_positions`를 재사용한다.
2. 마왕성 전용 정예와 중간보스를 `GameConfig.gd`의 전용 티어로 분리한다.
   - 기존 `dark_knight`, `wraith_knight`, `demon`, `gargoyle` 아트와 애니메이션을 우선 재사용한다.
   - 고유 키를 만들면 `Enemy.setup()`의 스프라이트 파일명 기반 애니메이션 재사용을 유지하고 `WalkAnimTest.gd`에도 사례를 추가한다.
3. 최종보스는 `Boss.gd`에 작은 상태 머신만 추가한다.
   - 처형선: 긴 직선 또는 부채꼴 전조 후 강타.
   - 소환 지휘: 읽을 수 있는 위치에 지휘관을 소환하고, 소환 중 본체 행동을 제한.
   - 왕좌/마지막 페이즈: 체력 게이트를 건너뛰지 못하게 하고 관문 수행량이 소환 수나 보호막에 반영.
4. 슬롯별 마왕성 전용 장비 3종을 추가한다.
   - 순수 공격력 상시 증가보다 관문, 소환수, 처형 패턴에 대응하는 규칙 변경 효과를 우선한다.
5. 기존 상단 목표 HUD와 층 텔레메트리를 연결한다.
   - 별도 대형 모달 UI를 추가하지 않는다.
   - 목표 키는 `castle_gates`처럼 범용 층 통계 구조에 맞춘다.

## 구조 규칙

- 목표 노드는 별도 `.gd`, 보스 패턴은 `Boss.gd`, 시간표·보상·HUD 연결만 `Main.gd`에 둔다.
- 모든 몬스터와 무기에 새 속성 태그를 전수 추가하지 않는다.
- 특정 속성이나 특정 무기가 없다고 목표 수행이 막히면 안 된다.
- 사용자 노출 문자열에 이모지를 넣지 않는다.
- 일반 분 단위 위협은 몬스터를 추가 소환하지 않는다. 수제 봉쇄전·보스 소환처럼 목표 규칙이 명확한 전투만 예외로 둔다.
- 새 UI/월드 아트가 정말 필요할 때만 PixelLab을 사용한다. 맵 오브젝트는 64px 캔버스와 nearest 필터를 유지한다.
- `_retro_backup`의 중복 UID 경고와 일부 종료 리소스 경고는 기존 경고다. 새 파서 오류·리소스 누락과 구분한다.

## 필수 검증

```powershell
$godotExe = 'C:\Users\kpo02\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe'

# 신규 마왕성 테스트와 애니메이션 재사용
& $godotExe --headless --path . --script res://tests/CastleSliceTest.gd
& $godotExe --headless --path . --script res://tests/WalkAnimTest.gd

# 기존 세로 슬라이스
& $godotExe --headless --path . --script res://tests/HellSliceTest.gd
& $godotExe --headless --path . --script res://tests/GraveyardSliceTest.gd
& $godotExe --headless --path . --script res://tests/GlacierSliceTest.gd
& $godotExe --headless --path . --script res://tests/VoidSliceTest.gd

# 메인 흐름
& $godotExe --headless --path . -- --autoshot --telemetry-test
& $godotExe --headless --path . -- --autoshot --map-selection-test
& $godotExe --headless --path . -- --autoshot --expedition-flow-test
```

커밋 전에는 모든 `tests/*Test.gd`, `git diff --check`, 실제 마왕성 프리뷰 스크린샷까지 확인한다.
