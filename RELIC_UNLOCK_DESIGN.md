# 유물(Relic) · 해금 · 시야 시스템 — 설계 구상안

> 상태: **구상 단계 (미구현)**. 뱀서(VS) 방식을 우리 게임에 이식하는 큰 작업이라, 먼저 방향·범위·우선순위를 확정하기 위한 문서.

---

## 0. 목표 3줄 요약
1. **시야 제한(어둠) + 횃불 광원** — 맵이 처음엔 캐릭터 주변만 보이고, 횃불이 주변을 밝힘.
2. **유물(Relic) 시스템** — 런 지속 특수효과 아이템. 대표: `노란 표식`(맵 전체 공개 + 진화조합표 공개).
3. **조건 달성 → 유물·캐릭터 영구 해금** — 도전과제 확장, 선택창에 잠금/해금 표시.

---

## 1. 재활용 가능한 기존 시스템 (새로 안 만들어도 됨)
| 기존 | 위치 | 활용 |
|---|---|---|
| 업적(ACHIEVEMENTS) `unlock` 필드 | Main.gd | 해금 조건 판정에 그대로 확장 |
| `Meta.gd` (user://meta.cfg) `ach` 섹션 저장 | Meta.gd | `unlocked_relics` / `unlocked_chars` 추가 저장 |
| 캐릭터 정의 `GameConfig.characters()` | GameConfig.gd | 잠금 플래그 + 해금 조건 필드 추가 |
| 파괴 오브젝트(횃불/관) | Breakable.gd | 광원 부여 대상 |
| 픽업(pickup) 시스템 | Pickup.gd | 유물 드랍/획득에 재사용 |

→ **완전 신규는 "시야 셰이더"와 "유물 데이터/획득 연출" 정도.** 나머지는 기존 확장.

---

## 2. 시스템별 설계

### A. 시야 제한 + 광원 (어둠 시스템)
- **어둠 오버레이**: 화면 전체를 어둡게 덮고, 광원 위치만 원형으로 뚫어 밝힘.
  - 구현안 1 (간단): `CanvasModulate`로 월드를 어둡게 + 플레이어/횃불에 `PointLight2D`(부드러운 원형 텍스처). Godot 2D 라이트로 자연스러운 광원.
  - 구현안 2 (셰이더): 어둠 레이어에 광원 좌표 배열 넘겨 radial punch-out. 유연하지만 광원 수 제한.
  - **권장: 구현안 1 (PointLight2D)** — 횃불마다 라이트 노드 붙이면 끝, 유지보수 쉬움.
- **밝기 파라미터**: 플레이어 시야 반경 R_player(예 260px), 횃불 반경 R_torch(예 180px).
- **횃불 역할 강화**: 지금은 그냥 부수는 물체 → **부수기 전엔 주변을 밝히는 광원**, 부수면 전리품 + 잠깐 폭발광.
- **난이도 연동**: 쉬움은 시야 넓게(R_player 크게), 어려움은 좁게 → 긴장감 차등.

### B. 유물(Relic) 시스템
- **정의**: 런 시작~종료까지 지속되는 특수효과 아이템. 레벨업 카드와 별개.
- **데이터 구조** (신규 `const RELIC_DEFS`):
  ```
  {"key":"yellow_sign", "name":"노란 표식", "desc":"맵의 모든 아이템 위치 + 진화 조합표 공개",
   "icon":"res://assets/items/relic_yellowsign.png", "unlock":"ach_survive10"}
  ```
- **대표 유물 후보** (뱀서 오마주 + 우리 게임 맞춤):
  | 유물 | 효과 | 해금 조건(예) |
  |---|---|---|
  | 노란 표식 | 맵 아이템 위치 공개 + **진화/유니온 조합표 공개** | 10분 생존 |
  | 은하 지도 | 미니맵 표시 | 첫 승리 |
  | 위대한 복음 | 획득 무기 즉시 만렙(1회) | 노데스 승리 |
  | 마녀의 눈물 | 사신 조기 소환(고위험 고보상) | 800킬 |
  | 검은 성배 | 흡혈 옵션 해금 | 특정 무기 진화 |
- **획득 방식**: (a) 맵 고정 위치 드랍 (b) 특정 상자 (c) 조건 달성 시 다음 런부터 적용 선택 가능.
- **"조합표 공개" 연동**: 앞서 일시정지에서 뺀 진화조건/유니온 표 → **노란 표식 보유 시에만** 다시 표시. (이미 코드에 훅 위치 메모 있음: `_build_pause_info`)

### C. 캐릭터 해금 시스템
- **캐릭터 defs에 필드 추가**: `"locked": true, "unlock":"ach_win_mage"` 등.
- **선택창 UI**: 잠긴 캐릭터는 실루엣 + 자물쇠 + 해금 조건 문구. 해금 시 "새 캐릭터 해금!" 연출.
- **해금 저장**: `Meta` `unlocked_chars` 섹션.
- **기본 해금**: 아처(무조건). 나머지는 조건.

### D. 해금 조건(도전과제) 확장
- 기존 ACHIEVEMENTS 재활용 + 유물/캐릭터용 항목 추가.
- 예: "메이지로 승리 → 프리스트 해금", "10분 생존 → 노란 표식 해금", "특정 무기 만렙 → 유물 해금".

### E. 디자인/아트 요소 (PixelLab)
- 유물 아이콘 N종 (relic_*.png)
- 어둠용 부드러운 광원 텍스처 1장 (또는 PointLight2D 기본)
- 미니맵 UI 프레임
- 해금 알림 배너/연출
- 캐릭터 선택창 잠금 실루엣 처리

### F. 저장 데이터 (Meta.cfg 확장)
```
[unlock]
relics = ["yellow_sign", "milky_map"]
chars  = ["archer", "mage"]
```

---

## 3. 구현 단계 (마일스톤) — 대략 규모
| 단계 | 내용 | 규모 |
|---|---|---|
| P1 | 유물 데이터 구조 + Meta 저장 + 해금 판정 로직 | 중 |
| P2 | 캐릭터 해금 UI(선택창 잠금/조건 표시) | 중 |
| P3 | 시야 제한(PointLight2D 어둠) + 횃불 광원 | 중~대 |
| P4 | 노란 표식 유물 → 맵 공개 + 조합표 공개 연동 | 소 |
| P5 | 유물 아트 생성 + 획득/해금 연출 | 중(아트) |
| P6 | 미니맵, 추가 유물 확장 | 선택 |

**추천 착수 순서**: P1 → P2 → P4 (데이터·해금·조합표는 저위험) → 그다음 P3 시야(가장 큰 시각 변화) → P5 아트.

---

## 4. 결정 사항 (확정 ✅ 2026-07-13)
1. **시야 제한 → 전 난이도 적용.** 모든 난이도에 어둠+횃불 광원. 난이도별 시야 반경만 차등(쉬움 넓게, 어려움 좁게).
2. **유물 획득 → 조건 달성 자동 해금.** 도전과제 달성 시 영구 해금, 다음 런부터 적용. (맵 드랍/상점 아님)
3. **해금 규모 → 대규모.** 목표: **캐릭터 10종+, 유물 8종+.** 아트·조건 설계 비중 큼.
4. **착수 시점 → 밸런스 먼저.** 지금은 XP·보스·거너 등 밸런스 다듬기 마무리 → 안정화 후 P1부터 시작.

### 확정에 따른 범위 조정 메모
- 전 난이도 시야 → **P3 완성도·QA 비중 커짐**, 난이도별 반경 테이블 필요.
- 대규모 해금 → **ACHIEVEMENTS 대폭 확장** + 신규 캐릭터 4종+ (PixelLab 아트 재가동) + 유물 아이콘 8장.
- **다음 액션**: 밸런스 안정화 완료 후 이 문서 P1부터 착수. 그때 캐릭터/유물 최종 목록 확정.
