# Phase ③ — VS 빌드완성 시스템 정리 (진화·유니온·콤보·아르카나·저주/축복)

> arrow-rpg의 뱀서(VS)→RPG 전환 마지막 설계 결정 문서.
> B블렌드(뱀서 50 : RPG 50, Halls of Torment형) 기준. 나머지 실작업은 Codex에서 진행.
> 작성: 2026-07-28 (Claude). 대상 파일: `Main.gd`, `GameConfig.gd`.

---

## 0. 지금까지 된 것 (전제)

RPG 코어는 이미 완성되어 커밋됨:

- **던전 선택 + 보스 처치 = 클리어** (기존 5스테이지=5던전, `map_stage>0` 분기, `DUNGEON_BOSS_TIME`)
- **속성 상성** (5속성 약점×1.5/저항×0.6, `attack_element`, `take_damage` 곱)
- **장착 무기 = 실제 주공격** (`GEAR_NOUN_ATTACK`, gear의 `weapon_kind`가 캐릭터 weapon1 대체)
- **지속 장비 보관함 + 대장간 강화** (`meta.stash`/`meta.loadout`, `_forge_*`)
- **스탯 분배** (레벨업당 포인트, `char_stats`, `_spend_stat_point`)
- **캐릭터별 고유 궁극기** (`CHAR_SKILLS`, 궁 5종 아키타입)
- **레거시 캠페인(30분 사신) dead code 삭제** (커밋 a418bd6)

남은 게 이 문서의 주제 = **VS 시절 "빌드 완성" 3종 + 런 변주 2종**을 RPG 문맥에서 어떻게 할지.

---

## 1. 대상 시스템 인벤토리 (규모·역할·충돌점)

| 시스템 | 위치 | 규모 | 역할(VS) | RPG와의 충돌 |
|---|---|---|---|---|
| **진화 EVO_RECIPE** | `Main.gd` const + ~15곳 참조 | 22종 | 무기 Lv8 + 필수 패시브 → 보스 상자서 진화 | 낮음. 무기 성장의 정점, gear 무기도 진화 가능 → **공존 자연스러움** |
| **유니온 UNION_DEFS** | const L266 + 완성/도감 | 6종 | 두 무기 Lv8 → 합체 신무기 | 중간. 여러 무기 만렙 전제 → gear 주무기 체제와 어긋남 |
| **콤보 COMBO_DEFS** | const L240 | 11종 | 두 무기 Lv3+ → 시너지 카드 | 중간. 카드-무기 다수 보유 전제 |
| **아르카나 ARCANA_DEFS** | const L223 + `_arcana_options` | 6종 (Lv8/16/24) | 마일스톤마다 강력 런버프(+25% dmg 등) | **높음.** 스탯분배·장비 어픽스와 효과 중복(공격력/쿨감/치명/흡혈) |
| **저주 다이얼** | `CURSE_*` L366, `sel_curse`, `_build_curse_gauge` | 0~5단계 | 캐릭터 선택 시 위험/보상 배수 | **높음.** 던전이 자체 난이도(몹티어+보스약점) 보유 → 역할 중복 |
| **축복 MODIFIERS** | const L347 | 7종 | 런 시작 전 1개 선택(변주) | 중간. 스탯/장비와 일부 중복, 하지만 "런 성격" 부여는 고유 |
| 유물 RELIC_DEFS | const L328 | 12종 | 업적 해금 영구 패시브 | 낮음. 영구성장 = 골드/대장간 루프와 어울림 → **유지 권장** |

**핵심 충돌 2개**: ① 아르카나 ↔ 스탯/장비(효과 중복) ② 저주 ↔ 던전 난이도(역할 중복).
나머지(진화/유니온/콤보)는 "인게임 무기 성장"으로 gear와 층위가 달라 공존 가능.

---

## 2. 대안 3종

### 대안 A — 공존 (Coexist) · 최소 변경

**무엇**: 전부 그대로 둔다. VS 빌드(무기+진화+유니온+콤보+아르카나) + RPG(gear+스탯+던전)를 병렬로.

- **KEEP**: 전부
- **CUT / REWIRE**: 없음
- **공수**: 0 (밸런스 튜닝 제외)
- **리스크**: 낮음(코드), 높음(설계) — 파워 출처 7~8개가 전부 곱연산 → **밸런스 지옥**, 시스템 과밀로 신규 유저 인지 부하, "뱀서냐 RPG냐" 정체성 흐림.
- **적합**: 콘텐츠 최대치가 우선이고, 밸런스는 나중에 손으로 잡을 각오가 있을 때.

### 대안 B — 중도 통합 (Consolidate) · **추천**

**무엇**: 충돌하는 2개만 정리하고, 무기 성장 재미(진화 등)는 살린다.

- **KEEP**: 진화(EVO_RECIPE), 유니온, 콤보, 유물 — 인게임 무기 성장 = 뱀서 손맛 핵심
- **CUT**: **아르카나** 제거 (효과가 스탯분배+장비 어픽스와 중복). 마일스톤 잭팟 감이 아쉬우면 → 아르카나를 "**던전 클리어 보상 카드**"로 재활용(런당 X개 아니라 던전 진행 보상)하는 것도 대안.
- **REWIRE**:
  - **저주 다이얼 → 던전 난이도 선택**으로 흡수. 던전 선택 화면에서 난이도(쉬움/보통/어려움/악몽)를 고르면 `curse_mult`류 배수를 그 난이도가 담당. `sel_curse` 로직 재활용(이미 배수 계산 있음), 다이얼 UI만 난이도 셀렉터로 교체.
  - **축복 MODIFIERS → 대장간 소모품/부적**으로 이동하거나, 던전 입장 전 "가호 1개 선택"으로 유지(런 성격 부여는 고유 가치라 완전 삭제보단 이동 권장).
- **공수**: 중간. 아르카나 제거(~6곳) + 저주→난이도 재배선(UI+연결) + 축복 이동.
- **리스크**: 낮음. 무기 성장(가장 큰 자산)은 보존, 중복 2개만 정리 → 정체성 선명 + 밸런스 축소.
- **적합**: "뱀서 손맛 나는 RPG" 목표에 가장 부합. **기본 추천.**

### 대안 C — 풀 RPG (Full pivot) · 과감

**무엇**: VS 빌드완성 시스템 대거 제거, 파워는 gear/스탯/스킬로 일원화.

- **KEEP**: 무기 레벨업(카드), 유물, 스탯/gear/던전/스킬
- **CUT**: **진화·유니온·콤보·아르카나·저주·축복 전부**. 무기는 레벨업만, 진화 없음. 파워 정점은 gear 레전더리 강화로.
- **REWIRE**: 도감/상자연출/업적("유니온 완성" 등)에서 제거된 시스템 참조 정리 필요.
- **공수**: 큼. EVO_RECIPE 15+곳, 도감(collection), 상자 룰렛, 업적 3종, DATA_TEST 등 대수술.
- **리스크**: 큼. "만렙→진화 잭팟"이라는 뱀서 최고의 쾌감을 버림 → 게임필 손실. 되돌리기 어려움.
- **적합**: 완전한 ARPG로 갈아탈 결심이 섰고, 뱀서색을 최대한 지울 때.

---

## 3. 추천 & 근거

**대안 B (중도 통합).**

- 가장 큰 자산인 **무기 성장(진화/유니온)**을 보존 → 뱀서 손맛 유지, 삭제 대수술 회피.
- 실제 충돌하는 **아르카나(효과 중복)·저주(역할 중복)** 딱 2개만 정리 → 파워 출처·인지 부하 감소, 밸런스 잡기 수월.
- 저주는 "삭제"가 아니라 **던전 난이도로 재활용** → 기존 배수 로직·연출 살림(ponytail: 재배선 > 재작성).
- B로 정리한 뒤 **Phase ④ 전면 밸런스 패스**(스탯 5중첩 등)를 하면 파워 출처가 정돈된 상태라 튜닝이 명확해짐.

> 사장님이 콘텐츠 최대치를 원하면 A, 완전 ARPG면 C. 애매하면 B.

---

## 4. Codex 작업 체크리스트 (대안 B 선택 시)

### 4-1. 아르카나 제거
- [x] `Main.gd` `ARCANA_MILE`, `ARCANA_DEFS` 삭제
- [x] `_arcana_options()`, `_take_arcana()` 삭제
- [x] `_populate_levelup` 내 `is_arcana` 분기와 전용 타이틀 제거 → 일반 카드로 통일
- [x] `arcana_count`/`arcana_taken` 런 상태 삭제, `arcana_lifesteal` 의존성 제거
- [x] 도감(collection) 아르카나 카운트 표기 없음 확인
- [x] 검은 성배·불멸의 심장 흡혈을 독립 `global_lifesteal`로 보존
- [x] 검증: 파싱 clean + `CARD_POOL_OK` + `XP_TEST PASS` + `EVO_TEST PASS` + `DATA_TEST PASS`

### 4-2. 저주 → 던전 난이도 흡수
- [x] 던전 선택 화면에 **쉬움/보통/어려움/악몽** 난이도 셀렉터 통합
- [x] `sel_curse` 삭제, 선택 난이도가 적·플레이어·보상 배수를 직접 결정
- [x] 캐릭터 화면의 저주 다이얼 제거, 빈 공간은 영웅 특성·시작 무기 상세로 교체
- [x] `CURSE_*` 상수와 전용 UI 함수 삭제, 유물·패시브 위협은 `run_pressure_mult`로 분리
- [x] 난이도별 골드·XP·장비 드롭·등급 운 보상 연결
- [x] 검증: `DIFFICULTY_OK` + `BALANCE_TEST PASS` + 실제 UI 렌더 캡처

### 4-3. 축복 이동 (선택)
- [x] `MODIFIERS` 유지, 진입 지점을 던전 준비 화면의 "가호 1개 선택"으로 이동
- [x] 사용되지 않던 별도 난이도·모디파이어 화면 제거

### 4-4. 마무리
- [x] 업적 "유니온 완성"(combo_master) 등은 유니온 유지하므로 그대로 보존
- [x] `tests/CardPoolTest.gd`: 일반/성장 소진 카드 선택 3장과 유물 흡혈 회귀 검사
- [x] `tests/DifficultyTest.gd`: 4단계 위험·보상 순서와 런 프로필 적용 검사
- [ ] 커밋 분리 권장: (1) 아르카나 제거 (2) 저주→난이도 (3) 축복 이동

---

## 5. 참고 — 주요 코드 좌표 (grep 기준, 삭제로 이동 가능)

```
EVO_RECIPE      Main.gd const L~115, 참조 ~15곳 (6113,6122,6153,6176,6184,6304,7915,9569,9626,9645,9656 + evo-test/DATA_TEST)
UNION_DEFS      Main.gd L266
COMBO_DEFS      Main.gd L240
ARCANA_DEFS     제거 완료(2026-07-28) / tests/CardPoolTest.gd에서 카드 풀 회귀 검사
CURSE_*         제거 완료(2026-07-28) / 런 중 유물·패시브 위협은 run_pressure_mult로 분리
MODIFIERS       던전 준비 화면 가호 버튼 / _start_game 적용부(mod.get(...))
RELIC_DEFS      Main.gd L328 (유지)
```

라인 번호는 편집으로 이동하므로 **심볼명으로 grep**할 것.
